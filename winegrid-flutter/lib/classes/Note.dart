class Note {
  final id;
  final title;
  final body;
  final attachments;
  final created_at;
  final is_report;

  Note(
      this.id,
      this.title,
      this.body,
      this.attachments,
      this.created_at,
      this.is_report,
      );
}
